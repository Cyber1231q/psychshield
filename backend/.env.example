DATABASE_URL=sqlite:///./psychshield.db
JWT_SECRET=change-this-to-a-long-random-string
JWT_EXPIRY_HOURS=8
PHISHTANK_CSV_PATH=../data/phishtank/online-valid.csv
ENRON_DATA_PATH=../data/enron/
MODEL_PATH=./saved_models/
CORS_ORIGINS=http://localhost:5173
# Optional: shares rate-limit state across instances instead of an
# in-memory dict. Leave unset for local dev — falls back automatically.
REDIS_URL=
